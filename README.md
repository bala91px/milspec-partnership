# Mil-Spec × 91Pixels — Live Partnership Deck

Client-facing interactive pitch deck (`index.html` + `media/`). Static site, served via GitHub Pages.
Not for search indexing (see `robots.txt` + `noindex` meta).
